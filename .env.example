# Copy to .env.local and add your key, then restart `npm run dev`.
#
# Read server-side only, in src/app/api/assistant/route.ts. It is never sent to
# the browser. .env.local is gitignored — do not commit a real key.
#
# Get one at https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-...
